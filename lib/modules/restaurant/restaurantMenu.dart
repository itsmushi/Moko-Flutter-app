import 'package:flutter/material.dart';

class RestaurantMenu extends StatelessWidget {
  static String route_name = "restaurant_menu";
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("restaurant menu"),
    );
  }
}
